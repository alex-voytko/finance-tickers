import Title from "../Title";
import Toggler from "../Toggler";

function AppBar() {
  return (
    <header className="app-bar">
      <Title />
      <Toggler />
    </header>
  );
}

export default AppBar;
